Getting Started
===============

.. meta::
   :description: How to install, build, and run Ananke.

.. note::

   This page is a placeholder. Content to be added.

Planned content:

* Installation from source (CMake + conan, see the root ``Readme.md`` for the
  current instructions).
* Running Ananke for the first time: repository scanning and the interactive
  base-directory prompt.
* Creating a first project with ``--new_app`` / ``--lang``.
* The main CLI flags and a minimal end-to-end workflow (depfile -> project).

See also :doc:`depfile_reference` for the project description format.
